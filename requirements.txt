# Exchange & Network
ccxt>=4.2.0
requests>=2.31.0
python-dotenv>=1.0.0

# Data Processing & Math
# Forcing NumPy < 2.0 to ensure scikit-learn works
numpy>=2.0.0
pandas>=2.0.0
scipy>=1.10.0

# Technical Analysis
# Removed the 'b0' suffix to avoid distribution lookup errors
pandas-ta-classic

# Neural Layer (Phase 4)
# Using slightly more flexible versions to help the pip resolver
scikit-learn>=1.4.0
sqlite-utils>=3.36

# Configuration & Utilities
PyYAML>=6.0.0
